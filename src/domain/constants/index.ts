import { AuthConstants } from './auth.constants';
import { MailConstants } from './mail.constants';
import { OtpConstants } from './otp.constants';

export class AppConstant {
  static readonly Auth = AuthConstants;
  static readonly Mail = MailConstants;
  static readonly Otp = OtpConstants;
}
